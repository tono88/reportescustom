<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Asignar códigos a PROVEEDORES (lista) -->
    <record id="act_assign_supplier_codes" model="ir.actions.server">
        <field name="name">Asignar códigos internos a Proveedores</field>
        <field name="state">code</field>
        <field name="model_id" ref="base.model_res_partner"/>
        <field name="binding_model_id" ref="base.model_res_partner"/>
        <field name="binding_view_types">list</field>
        <field name="code">
            action = env['res.partner'].browse(context.get('active_ids', [])).action_assign_all_supplier_codes()
        </field>
    </record>

    <!-- Asignar códigos a CLIENTES (lista) -->
    <record id="act_assign_customer_codes" model="ir.actions.server">
        <field name="name">Asignar códigos internos a Clientes</field>
        <field name="state">code</field>
        <field name="model_id" ref="base.model_res_partner"/>
        <field name="binding_model_id" ref="base.model_res_partner"/>
        <field name="binding_view_types">list</field>
        <field name="code">
            action = env['res.partner'].browse(context.get('active_ids', [])).action_assign_all_customer_codes()
        </field>
    </record>
</odoo>
